# bootstrap/packer/http/preseed.cfg
# Debian 12 Bookworm netinst autoinstall config.

# Locale
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# Network - uses DHCP at install time (Packer serves preseed over HTTPIP)
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string soc-lab-template
d-i netcfg/get_domain string soc.local

# Mirror - Debian official
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# Clock + Time Zone
d-i clock-setup/utc boolean true
d-i time/zone string Etc/UTC
d-i clock-setup/ntp boolean true

# Partitioning - single primary, no LVM, no swap partition
d-i partman-auto/method string regular
d-i partman-auto/disk string /dev/sda
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Root account - disabled (use sudo)
d-i passwd/root-login boolean false

# Vagrant user
d-i passwd/user-fullname string vagrant
d-i passwd/username string vagrant
d-i passwd/user-password password vagrant
d-i passwd/user-password-again password vagrant
d-i user-setup/allow-password-weak boolean true
d-i user-setup/encrypt-home boolean false

# APT - minimal at install time; provisioner installs additional packages
d-i apt-setup/services-select multiselect security, updates
d-i apt-setup/security_host string security.debian.org
tasksel tasksel/first multiselect ssh-server, standard
d-i pkgsel/include string openssh-server sudo
d-i pkgsel/upgrade select safe-upgrade
d-i pkgsel/update-policy select none

# Skip the popularity contest
popularity-contest popularity-contest/participate boolean false

# Boot loader
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string /dev/sda

# Finish
d-i finish-install/reboot_in_progress note

# After install: allow vagrant to sudo without password
d-i preseed/late_command string \
  echo "vagrant ALL=(ALL) NOPASSWD: ALL" > /target/etc/sudoers.d/vagrant ; \
  chmod 0440 /target/etc/sudoers.d/vagrant
