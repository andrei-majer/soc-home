# Managed by Ansible
filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /var/log/suricata/eve.json
  json.keys_under_root: true
  json.add_error_key: true
  json.message_key: event_type

output.logstash:
  hosts: ["{{ filebeat_logstash_host }}"]

processors:
  - add_host_metadata: ~
