# OpenCanary SMB integration — route full_audit logs to a dedicated file
# that the SambaLogWatcher tails. Drop after match so they don't also land
# in /var/log/syslog.
local5.notice  -/var/log/samba-audit.log
& stop
