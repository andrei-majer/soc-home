---
# soc-ansible/inventory/host_vars/suricata-120/vars.yml
# Non-secret host vars. Secrets stay in vault.yml.

backup_enable_velociraptor: true
backup_enable_grafana: true
backup_enable_evebox: true   # EveBox is not Ansible-managed — this is the DR path for evebox.yaml
