/backups/
files/originals/
*.retry
*.pyc
__pycache__/
.vault_pass
*.bak.*
# Velociraptor server config holds CA/server/GUI private keys — never commit it.
# Generate it on .120 at deploy time (template + vault) per
# docs/runbooks/velociraptor-ca-rotation.md.
roles/suricata/files/server.config.yaml
