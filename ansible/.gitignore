/backups/
files/originals/
*.retry
*.pyc
__pycache__/
.vault_pass
*.bak.*
# Velociraptor server config (roles/suricata/files/server.config.yaml) holds CA/server/GUI
# private keys. It is committed VAULT-ENCRYPTED (AES256) and the deploy task auto-decrypts
# on copy. After rotation (docs/runbooks/velociraptor-ca-rotation.md) re-capture + re-encrypt.
